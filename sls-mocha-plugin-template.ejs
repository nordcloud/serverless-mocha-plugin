'use strict';
// tests for <%= functionName %>
// Generated by serverless-mocha-plugin

const mod               = require('../<%= functionPath %>');
const mochaPlugin       = require('serverless-mocha-plugin');
const lambdaWrapper     = mochaPlugin.lambdaWrapper;
const expect            = mochaPlugin.chai.expect;

const liveFunction = {
  region: process.env.SERVERLESS_REGION,
  lambdaFunction: process.env.SERVERLESS_PROJECT + '-<%= functionName %>'
};

const wrapped = lambdaWrapper.wrap(mod, { handler: '<%= functionName %>' });

describe('<%= functionName %>', () => {
  before(function (done) {
//  lambdaWrapper.init(liveFunction); // Run the deployed lambda

    done();
  });

  it('implement tests here', (done) => {
    wrapped.run({}, (err, response) => {
      done('no tests implemented');
    });
  });
});
