// Proxy
const path = require('path');

const webpackDir = path.join(process.env.MOCHA_PLUGIN_TEST_DIR,
  '../',
  'node_modules',
  'copy-webpack-plugin');
/* eslint-disable import/no-dynamic-require */
module.exports = require(webpackDir); // eslint-disable-line global-require
/* eslint-enable import/no-dynamic-require */
